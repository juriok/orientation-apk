<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Track recording must survive the screen going off, which needs a foreground service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Eyes-free confirmation that a control was reached, without looking at the phone. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Orientacija">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="fullUser"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".custom.CustomMapsActivity"
            android:exported="false"
            android:label="@string/custom_maps"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".custom.CalibrationActivity"
            android:exported="false"
            android:label="@string/calibrate"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:parentActivityName=".custom.CustomMapsActivity" />

        <activity
            android:name=".data.WaypointsActivity"
            android:exported="false"
            android:label="@string/waypoints"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".data.TracksActivity"
            android:exported="false"
            android:label="@string/tracks"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".data.CoursesActivity"
            android:exported="false"
            android:label="@string/courses"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".HelpActivity"
            android:exported="false"
            android:label="@string/help"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".map.OfflineActivity"
            android:exported="false"
            android:label="@string/offline_manager"
            android:parentActivityName=".MainActivity" />

        <service
            android:name=".data.TrackRecordingService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
